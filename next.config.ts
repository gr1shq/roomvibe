import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['m.media-amazon.com', 'www.temu.com'], // add more domains if needed
  },
};

export default nextConfig;
